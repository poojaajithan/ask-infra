# Test-only overrides — these values are merged with (and override) src/main/resources/application.properties
# during test execution only.

# Provide a placeholder API key so Spring AI autoconfiguration can start in tests.
# No real calls are made to Gemini during context-load or JPA slice tests.
spring.ai.google.genai.api-key=test-placeholder-key
